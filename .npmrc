# Enable hoisting for better compatibility
shamefully-hoist=true

# Allow build scripts for Prisma packages (required for client generation)
enable-pre-post-scripts=true

# Approve build scripts for specific packages
unsafe-perm=true
