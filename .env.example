# Environment Variables Template for ArtsyGhana

# Database Configuration
# PostgreSQL connection string (use SSL in production)
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# NextAuth Configuration
# Generate a strong secret: `openssl rand -base64 32`
NEXTAUTH_SECRET="your-super-secret-key-here-generate-using-openssl"
#NEXTAUTH_URL should use HTTPS in production
NEXTAUTH_URL="http://localhost:3000"

# Paystack Payment Integration
# Get your keys from https://dashboard.paystack.com/#/settings/developer
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY="pk_test_xxxxxxxxxxxx"
PAYSTACK_SECRET_KEY="sk_test_xxxxxxxxxxxx"

# Cloudinary Image Upload
# Get your credentials from https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME="your_cloud_name"
CLOUDINARY_API_KEY="your_api_key"
CLOUDINARY_API_SECRET="your_api_secret"

# Optional: Admin Invite Code (uncomment to require invite codes for signup)
# ADMIN_INVITE_CODE="your-secure-invite-code"

# Security Notes for Production:
# 1. NEVER commit .env or .env.local to Git
# 2. Use strong, unique secrets for NEXTAUTH_SECRET
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Use environment-specific values (dev/staging/prod)
# 5. Ensure DATABASE_URL uses SSL in production
# 6. Keep Paystack secret keys secure - never expose to client
# 7. Set NEXTAUTH_URL to your production domain with HTTPS
